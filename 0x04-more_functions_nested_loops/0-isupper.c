#include "main.h"

/**
 * _isupper - Checks for the uppercase character
 *
 * Return: 1 if c is uppercase, else 0
 */

int _isupper(int c)
{
	c = _isupper(c);

	if (c == 65)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
